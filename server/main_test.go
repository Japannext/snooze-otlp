package server

/*
import (
    "testing"
    "net/http"
    "net/http/httptest"
)
*/
/*
func TestExport(t *testing.T) {

    svr := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
        //fmt.Frintf(w, expected)
    }))
    defer svr.close()



}
*/
